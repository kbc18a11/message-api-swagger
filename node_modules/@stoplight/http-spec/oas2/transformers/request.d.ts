import { DeepPartial, IHttpOperationRequest } from '@stoplight/types';
import { Parameter, Spec } from 'swagger-schema-official';
export declare function translateToRequest(document: DeepPartial<Spec>, parameters: Parameter[], consumes: string[]): IHttpOperationRequest;
